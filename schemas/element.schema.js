// const Joi = require('joi');

// const id = Joi.number().integer();
// const name = Joi.string().min(3).max(150);
// const image = Joi.string().uri();
// const description = Joi.string().min(10).max(150);
// const type = Joi.string().min(3).max(15);
// const measure = Joi.string().min(3).max(15);


// const createElementSchema = Joi.object({
//   name: name.required(),
//   image: image.required(),
//   description: description.required(),
//   type: type.required(),
//   measure: measure.required()
// });

// const updateElementSchema = Joi.object({
//   name,
//   image,
//   description,
//   type,
//   measure
// });

// const getElementSchema = Joi.object({
//   id: id.required(),
// });

// module.exports = { createElementSchema, updateElementSchema, getElementSchema }
